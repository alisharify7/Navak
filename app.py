from navak import app

# this

if __name__ == "__main__":
    app.run(
        debug=True,
        port=8080,
        # host="192.168.70.70",
        # ssl_context="adhoc"
        # ssl_context=("localhost.crt", "localhost.key")

    )
